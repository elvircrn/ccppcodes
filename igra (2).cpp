#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <algorithm>

using namespace std;

int Pokusaj(int k);

	/* Ovdje napisite svoje rjesenje, pri tome smijete i trebate pozivati     *
     * funkciju koja je implementirana u 'stub.cpp' sa sljedecim prototi    void ZapocniIgru(int a, int b)
    {
        long long int left = a, right = b, pivot;

        while (left <= right)
        {
            pivot = (left + right) / 2;

            int pokusaj = Pokusaj (pivot);

            if (pokusaj == -1)
                left = pivot + 1;
            else if (pokusaj == 1)
                right = pivot - 1;
            else
                break;
        }
om:   *
     *      int Pokusaj(int k);                                               */

}
