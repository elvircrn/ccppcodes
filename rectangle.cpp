#include <iostream>
#include <cstdlib>
#include <cstdio>

using namespace std;

int mem [2000] [2000], n, x, y, s;

void set (int posX, int posY, int size)
{
    for (int i = 0; i < s; i++)
    {

    }

    return;
}

int main ()
{
    scanf ("%d", &n);
    while (n--)
    {
        scanf ("%d %d %d", &x, &y, &s);
    }

    return 0;
}
