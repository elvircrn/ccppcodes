#include <iostream>
#include <cstdio>
#include <cstdlib>

using namespace std;

int t, A, B;

int main () {
    scanf ("%d", &t);
    while (t--) {
        scanf ("%d %d", &A, &B);
        printf ("%d\n", B);
    }
    return 0;
}
