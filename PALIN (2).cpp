#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <algorithm>
#include <vector>
#include <string>
#include <cstring>

using namespace std;

short dp [3] [5010], n;
char name [5010];

int main()
{
    char c;
    scanf ("%d", &n);

    while (!isalpha (c = getchar()));

    name [0] = c;

    int index = 0;

    while (isalpa (name [index++] = getchar()));

    for (int i = 0; i < index; i++)
    {
        for (int j = 0; j < index; j++)
        {

        }
    }

    return 0;
}
