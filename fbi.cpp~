#include <iostream>
#include <string>

using namespace std;

int main ()
{
    string a;
    int brojac=0;
    getline(cin,a);
    for (int i=0;i<a.length();i++)
    {
        if (a.substr(i,3)=="FBI")
        {
            brojac++;
        }
    }
    cout << brojac;
    return 0;
}
