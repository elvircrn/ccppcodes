#include <iostream>
#include <cstdio>
#include <cstdlib>

using namespace std;

int A, B, C, D;

int main () {
    scanf ("%d %d %d", &A, &B, &C);

    for (int i = 0; i < A; i++) {
        for (int j = 0; j < B; j++) {
            scanf ("%d", &D);
        }
    }

    return 0;
}
